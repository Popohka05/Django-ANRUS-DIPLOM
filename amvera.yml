meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12

build:
  requirementsPath: requirements.txt
  useCache: false

run:
  command: sh deploy/amvera_start.sh
  persistenceMount: /data
  containerPort: 80
