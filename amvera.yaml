meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12

build:
  requirementsPath: requirements.txt
  useCache: false

run:
  scriptName: start_amvera.py
  persistenceMount: /data
  containerPort: 80
